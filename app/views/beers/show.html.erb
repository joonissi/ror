<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery.name %>
</p>

<% if @beer.ratings.empty? %>
 beer has not yet been rated!
<% else %>
 <%= "Has " + pluralize(@beer.rating_average[0], 'rating', 'ratings') + ", average " +  @beer.rating_average[1].to_s %>
<% end %>

<br>
<br>

<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%= link_to 'Back', beers_path %>
